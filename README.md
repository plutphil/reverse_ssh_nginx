# reverse_ssh_nginx Home Assistant
reverse ssh tunnel and nginx with self-signed certificate for Home Assistant
 + No need for let's Encrypt
 + fast reverse tunnel
 + subdomain not listed on crt.sh
+ - less secure because you have to verify certificate
